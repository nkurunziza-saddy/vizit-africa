# Docker Resources

This directory is reserved for shared Docker scripts, CI/CD pipeline configs, or production reference files.

The main development orchestration is handled by the `docker-compose.yml` at the project root.
